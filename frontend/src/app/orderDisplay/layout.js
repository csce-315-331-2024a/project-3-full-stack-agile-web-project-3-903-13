export const metadata = {
  title: 'Order Display',
  description: 'Order Display',
};
  
  export default function RootLayout({ children }) {
   return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  